spring:
  application:
    name: job-description-api
  cloud:
    kubernetes:
      config:
        enabled: true
        name: job-description-api
        namespace: local-dev
        sources:
          - namespace: local-dev
            name: alaitp-shared
          - namespace: local-dev
            name: alaitp-job-desc
